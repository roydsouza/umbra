---
description: Perform an awatcher session checkpoint
---

1. Update `SYNC_LOG.md` with recent advancements.
2. Commit and push changes to the repository.
   // turbo
3. `git -C /Users/rds/antigravity/umbra add awatcher && git -C /Users/rds/antigravity/umbra commit -m "checkpoint: awatcher updates" && git -C /Users/rds/antigravity/umbra push`
